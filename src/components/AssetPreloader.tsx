import { useTexture } from "@react-three/drei"

const PRELOAD_ASSETS = [
  // List assets here
]

export function AssetPreloader() {
  // Rearchitected preloader: allows declarative preloading or null render
  // useTexture(PRELOAD_ASSETS)
  return null
}
